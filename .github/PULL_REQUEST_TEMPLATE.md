## What

<!-- One sentence. This repository takes documentation and installer-mirror
     changes; CLI source changes are not accepted here. -->

## Checklist

- [ ] `python3 scripts/check-docs.py` passes
- [ ] `scripts/install.sh` only changed if the channel already serves this version
- [ ] Nothing under `docs/reference/` was hand-edited
