# @camy/cli-PLATFORM

The `camy` binary for PLATFORM_HUMAN. Install the `camy` package instead;
it depends on this one and picks the right platform for you.

Documentation and releases: [github.com/trycamy/cli](https://github.com/trycamy/cli).
